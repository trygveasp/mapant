language: python
python:
  - "3.5"
  - "3.6"
  - "3.7"
  - "3.8"
os: linux

before_install:
  - sudo apt-get -y install imagemagick

install:
  pip install -r requirements.txt

# command to run tests
script:
  - nosetests
